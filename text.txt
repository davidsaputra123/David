<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kotak Pesan Rahasia David</title>
    <style>
        /* CSS untuk Tampilan */
        body {
            background-color: #000000; /* Latar Belakang Hitam */
            color: #ffffff; /* Warna teks putih */
            font-family: sans-serif;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
        }

        h1 {
            font-size: 3em;
            color: #ffffff; /* Tulisan 'David' Putih */
            margin-bottom: 30px;
            text-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
        }

        .container {
            width: 100%;
            max-width: 500px;
            background-color: #1a1a1a;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(255, 255, 255, 0.2);
        }

        textarea {
            width: 100%;
            height: 150px;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #444;
            background-color: #333;
            color: #fff;
            border-radius: 5px;
            resize: vertical;
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #6a0dad; /* Warna tombol ungu */
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1.1em;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #8c2cdc;
        }

        #status-message {
            margin-top: 20px;
            font-weight: bold;
        }
    </style>
</head>
<body>

    <h1>David</h1>

    <div class="container">
        <h2>Kirim Pesan Anonim</h2>
        <textarea id="anonymous-message" placeholder="Tulis pesan rahasiamu di sini..." required></textarea>
        <button onclick="kirimPesan()">Kirim Pesan</button>
        <p id="status-message"></p>
    </div>

    <script>
        function kirimPesan() {
            const message = document.getElementById('anonymous-message').value.trim();
            const statusElement = document.getElementById('status-message');

            if (message === "") {
                statusElement.textContent = "Pesan tidak boleh kosong!";
                statusElement.style.color = 'red';
                return;
            }

            // --- FUNGSI PENGIRIMAN PESAN DAN PELACAKAN (TIDAK BERFUNGSI SENDIRI) ---
            
            /* CATATAN PENTING:
               Di sini seharusnya ada kode JavaScript yang berkomunikasi dengan SERVER (Backend)
               untuk menyimpan pesan dan MENCATAT data teknis (seperti IP address) pengirim
               untuk tujuan 'pelacakan'.
               
               Kode di bawah ini HANYA SIMULASI. Tidak ada pesan yang terkirim atau tercatat.
            */

            console.log("Pesan yang dicoba kirim:", message);
            
            // SIMULASI BERHASIL
            statusElement.textContent = "Pesan terkirim! David akan membacanya.";
            statusElement.style.color = 'lightgreen';
            document.getElementById('anonymous-message').value = ''; // Kosongkan area teks

            // Jika Anda memiliki URL API (Endpoint) yang sudah siap (misalnya dari PHP/Node.js):
            /*
            fetch('URL_API_ANDA_DI_SINI', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ message: message })
            })
            .then(response => response.json())
            .then(data => {
                // Tampilkan pesan sukses dari server
            })
            .catch(error => {
                // Tampilkan pesan error
            });
            */
        }
    </script>

</body>
</html>